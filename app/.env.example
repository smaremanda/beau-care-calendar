# Copy to .env for local dev. Do NOT commit the real .env.
APP_PIN=1234
SECRET_KEY=change-me-to-a-random-string
SHEET_ID=REPLACE_WITH_SHEET_ID
# GOOGLE_CREDENTIALS is only needed in production (Railway).
# Locally, place the service-account key in credentials.json instead.
